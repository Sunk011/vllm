编译时需区分 CUDA 与 ROCm 构建：`USE_ROCM` 宏控制代码分支；ROCm 路径下可通过环境变量 `VLLM_ROCM_SLEEP_MEM_CHUNK_SIZE`（MB 单位）调整默认分块大小。每个 `.cpp`/`.cu` 文件独立生成一个 Python 扩展模块（`_C`、`_qutlass_C`、`fs_io_C`、`cumem_allocator`、`spinloop`）。
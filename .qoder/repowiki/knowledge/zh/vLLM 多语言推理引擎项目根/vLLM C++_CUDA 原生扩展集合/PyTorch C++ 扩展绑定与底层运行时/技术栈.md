PyTorch C++ Extension（`torch::library`、`TORCH_LIBRARY_FRAGMENT`、`REGISTER_EXTENSION`）、CUDA Driver API（cuMem*、cuCtx*）、ROCm HIP（hipIpcMemHandle_t、USE_ROCM 分支）、Python C API（`PyModuleDef`、`PyArg_ParseTuple`、GIL 管理）、ATen/c10 类型系统（`torch::Tensor`、`ScalarType`）。
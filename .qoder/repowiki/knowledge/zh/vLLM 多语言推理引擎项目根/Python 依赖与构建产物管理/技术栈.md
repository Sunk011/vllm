使用 `uv pip compile` 作为依赖锁定工具链，配合 `--index-strategy unsafe-best-match`、`--torch-backend`、`--python-platform`、`--python-version` 参数为不同目标平台生成确定性锁定文件；依赖源同时包含 PyPI 和自定义 `--extra-index-url`（flashinfer.ai、download.pytorch.org/whl/xpu）。
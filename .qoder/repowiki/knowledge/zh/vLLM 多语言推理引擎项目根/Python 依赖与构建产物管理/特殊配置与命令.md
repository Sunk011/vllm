测试锁定文件通过 uv 命令生成，例如：`uv pip compile requirements/test/cuda.in -o requirements/test/cuda.txt --index-strategy unsafe-best-match --torch-backend cu130 --python-platform x86_64-manylinux_2_28 --python-version 3.12`；CPU 目标使用 `--torch-backend cpu`。新增依赖应写入 `.in` 源文件而非直接修改 `.txt` 锁定文件。
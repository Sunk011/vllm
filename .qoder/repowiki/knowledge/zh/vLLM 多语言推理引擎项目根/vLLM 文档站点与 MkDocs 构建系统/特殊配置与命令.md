文档构建通过 MkDocs 的 `on_startup` 钩子在 `build`/`gh-deploy`/`serve` 时自动执行：`generate_argparse.py` 生成 `docs/generated/argparse/*.inc.md`，`generate_examples.py` 生成 `docs/examples/<category>/*.md`；Windows 下所有文件读写显式指定 `encoding="utf-8"`。
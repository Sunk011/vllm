- 注意力基准：`cd benchmarks/attention_benchmarks && python benchmark.py --config configs/mla_decode.yaml`
- 内核基准：`python benchmarks/kernels/benchmark_fp8_gemm.py --models meta-llama/Llama-3.1-8B-Instruct --tp-sizes 1`
- 多轮对话基准：需先安装 `benchmarks/multi_turn/requirements.txt`，再运行 `benchmark_serving_multi_turn.py`
- 自动调参：`bash benchmarks/auto_tune/auto_tune.sh`，支持通过环境变量 `MODEL`、`TP`、`NUM_SEQS_LIST`、`MAX_LATENCY_ALLOWED_MS` 等注入参数
- 旧版顶层脚本已废弃，应改用 `vllm bench latency|serve|throughput`。
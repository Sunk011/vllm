pip install -e . 或 pip install vllm 触发 setup.py 自动检测 VLLM_TARGET_DEVICE 并调用 CMake 编译原生扩展；直接 cmake 构建需先设置 VLLM_PYTHON_EXECUTABLE 并执行 cmake --build . --target install；预编译 wheel 可通过 VLLM_USE_PRECOMPILED=1 跳过本地编译；Rust 前端可通过 VLLM_REQUIRE_RUST_FRONTEND 控制是否构建。
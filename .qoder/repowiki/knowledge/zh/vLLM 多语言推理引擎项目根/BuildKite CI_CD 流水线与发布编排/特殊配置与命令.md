发布流水线需通过 BuildKite UI 输入 release-version 并可选择 NIGHTLY=1 触发 nightly 流程；ROCm 构建依赖 ECR 缓存键（`cache-rocm-base-wheels.sh key`）和 S3 缓存状态检查；macOS arm64 wheel 因体积限制需分片上传后在另一节点重组校验 SHA256；所有 ECR/DockerHub 推送通过 AWS IAM Web Identity 或 docker-login 插件完成认证。
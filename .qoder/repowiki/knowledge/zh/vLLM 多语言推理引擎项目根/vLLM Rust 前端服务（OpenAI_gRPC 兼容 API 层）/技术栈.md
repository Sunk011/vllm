Rust 2024 edition；HTTP 使用 axum + hyper-util（自定义 HTTP/1 连接处理以支持 keep-alive 与优雅关闭）；gRPC 使用 tonic 0.14 + prost；进程间通信通过 zeromq + rmp-serde（MessagePack）；模板渲染使用 minijinja；TLS 通过 tls-listener + tokio-openssl；内存分配器默认 mimalloc；构建系统使用 Cargo workspace resolver=3。
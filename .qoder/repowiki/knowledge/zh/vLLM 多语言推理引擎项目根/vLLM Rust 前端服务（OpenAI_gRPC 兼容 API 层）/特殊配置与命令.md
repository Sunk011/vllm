工作区根目录执行 `./build_rust.sh` 可独立构建；通过环境变量 `VLLM_USE_RUST_FRONTEND=1` 让 Python vLLM 启动时加载 Rust 前端；standalone 模式需设置 `--data-parallel-size-local 0` 并配合 `--headless`、`--data-parallel-address`、`--data-parallel-rpc-port`、`--data-parallel-size` 参数；gRPC 端口由 `grpc_port` 配置项控制，默认关闭。
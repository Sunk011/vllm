多数示例直接 `python <script>.py` 运行；部分需要额外依赖或前置服务，如 Gradio 示例需 `pip install --upgrade gradio` 并先启动 `examples/applications/api_server/server.py`；Helm 部署需 `helm install chart-vllm ./chart-helm`；Ray 示例通过 `run_cluster.sh` 启动集群；部分脚本附带 shell 引导（如 `disagg_*.sh`、`cpu_offload_lmcache_mp.sh`）。
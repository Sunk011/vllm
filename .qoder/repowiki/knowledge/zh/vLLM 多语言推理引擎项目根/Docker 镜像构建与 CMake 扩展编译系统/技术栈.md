Docker BuildKit + docker buildx bake（HCL 配置）、CMake 3.x（FetchContent、Python 模块绑定、CUDA/HIP 语言支持）、PyTorch 扩展机制（torch.utils.cpp_extension、hipify）、uv（替代 pip 进行依赖安装与缓存）、sccache（Rust/C++/CUDA 增量编译缓存）、hipify_torch（CUDA→HIP 转换）。
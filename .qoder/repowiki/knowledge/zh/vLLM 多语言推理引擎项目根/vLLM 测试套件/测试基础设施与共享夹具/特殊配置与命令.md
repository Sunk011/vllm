测试运行依赖 `pytest`，分布式测试需通过 `dist_init` fixture 初始化 NCCL 后端并使用临时文件作为 FileStore；ROCm/XPU 平台下 `requires_spawn_multiprocessing()` 强制使用 spawn 而非 fork 启动子进程；GPU 内存泄漏检测在 `RemoteVLLMServer._wait_for_gpu_memory_release` 中默认等待 120 秒，超时直接抛错。